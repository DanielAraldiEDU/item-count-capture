# Item Count Capture

Uma aplicação que realizar a contagem e pesagem de itens em esteiras.

### Como executar?

Rodar o seguinte comando no terminal:
```bash
gcc -o main.exe main.c -lpthread -lrt && gcc -o display.exe display.c -lpthread -lrt
```
